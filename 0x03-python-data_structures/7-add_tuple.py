#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
#    t_new = ((tuple_a[0] + tuple_b[0]), (tuple_a[1] + tuple_b[1]))
#    for i in
#    if len(m
        k = 0
#        t_new = (((tuple_a[i] + tuple_b[i]), (tuple_a[i] + tuple_b[i]))
#        if t_new[i]:
        t_new = tuple()
        for a, b in zip(tuple_a[:2], tuple_b[:2])) 
            if a and b:
                t_new += a + b,)
        return t_new
